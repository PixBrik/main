/** Native stub — photoreal turntables are generated on web only. */

import type { VoxelModel } from './voxelFox';

export const TURNTABLE_3D_WIDTH = 640;
export const TURNTABLE_3D_HEIGHT = 480;

export function renderBrickTurntable3D(
  _model: VoxelModel,
  _frames = 4,
  _width = TURNTABLE_3D_WIDTH,
  _height = TURNTABLE_3D_HEIGHT,
): string[] {
  return [];
}
